import { Routes } from '@angular/router';

// Substitui demo-routes.ts via fileReplacements (angular.json) no build de produção:
// as páginas de exemplo (dashboard/charts/ui-elements/maps) ficam fora do bundle.
export const demoRoutes: Routes = [];
